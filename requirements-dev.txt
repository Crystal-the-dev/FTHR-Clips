# Pinned development tools; install alongside requirements-alpha.txt.
# These tools support testing, linting, and packaging and are not bundled.

# Test
pytest==9.1.1
pytest-qt==4.5.0
iniconfig==2.3.0          # via pytest
pluggy==1.6.0             # via pytest
packaging==26.1           # via pytest / pyinstaller
colorama==0.4.6           # via pytest on Windows
Pygments==2.20.0          # via pytest
typing_extensions==4.15.0 # via pytest-qt

# Lint
ruff==0.16.1

# Packaging (Windows + Linux bundles)
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.4
altgraph==0.17.5          # via pyinstaller
setuptools==82.0.1        # via pyinstaller
pefile==2024.8.26         ; sys_platform == 'win32'   # via pyinstaller
pywin32-ctypes==0.2.3     ; sys_platform == 'win32'   # via pyinstaller

# NOTE: imageio-ffmpeg is deliberately absent (AUDIT-005). Its bundled binary
# is a GPLv3 FFmpeg build. The app shells out to the LGPL ffmpeg.exe /
# ffprobe.exe shipped next to the engine — see FTHR_UI/core/ffmpeg_tools.py.
# If you find it in a lock file again, that is a licence regression.
